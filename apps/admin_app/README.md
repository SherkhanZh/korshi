# Korshi — Chairman App (`apps/admin_app`)

Flutter app for KSK **chairmen** to triage resident reports, post updates and
announcements, run polls, and update request statuses.

Status: **planned** — placeholder. Scaffold with:

```bash
cd apps
flutter create --org com.korshi admin_app
```

It should depend on `packages/shared` (once extracted) for models, theme, and
localization so it stays visually and structurally consistent with the resident
app in `apps/client`.
